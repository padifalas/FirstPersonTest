using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ToyWeight : MonoBehaviour
{
    public float weightValue = 1f; // Set this value according to the object's weight
    
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
